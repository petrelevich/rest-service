jib {
    container.creationTime = 'USE_CURRENT_TIMESTAMP'
    from {
        image = 'bellsoft/liberica-openjdk-alpine-musl:17.0.2-9'
    }
    to {
        tags = ['v10', 'latest']
        image = 'registry.gitlab.com/petrelevich/dockerregistry/rest-hello'
        auth {
            username = gitlabUser
            password = gitlabPassword
        }
    }
}
// ./gradlew jib

//docker login registry.gitlab.com
//docker run -p 8080:8080 registry.gitlab.com/petrelevich/dockerregistry/rest-hello

//docker push registry.gitlab.com/petrelevich/dockerregistry/rest-hello:latest
